# For Vercel serverless (api/index.py). Backend code is in ../backend.
# Database: Firestore (Firebase). No PostgreSQL needed.
# Vercel uses native ASGI - no Mangum needed.
fastapi==0.115.0
pydantic==2.9.0
firebase-admin>=6.0.0
python-multipart>=0.0.6
